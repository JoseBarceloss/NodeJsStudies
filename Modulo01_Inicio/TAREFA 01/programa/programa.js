const numero1 = 6;
const numero2 = 23;

const resultado = `O resultado de ${numero1} + ${numero2} é ${numero1 + numero2}`;

console.log(resultado);


// para rodar o resultado no node e so digitar no terminal node programa.js